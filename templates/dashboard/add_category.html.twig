{% extends 'dashboard/base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    {{ form_start(Form) }}

    <div>
        <label for="price" class="block text-sm font-medium text-gray-700">{{ form_label(Form.title, 'Titre') }}</label>
        <div class="mt-1 relative rounded-md shadow-sm">
            {{ form_widget(Form.title, {'attr': {'class': 'focus:ring-indigo-500 focus:border-indigo-500 block w-full pl-7 pr-12 sm:text-sm border-gray-300 rounded-md'}}) }}
        </div>
    </div>

    <div>
        <label for="price" class="block text-sm font-medium text-gray-700">{{ form_label(Form.pathCategory, 'Chemin URL') }}</label>
        <div class="mt-1 relative rounded-md shadow-sm">
            {{ form_widget(Form.pathCategory, {'attr': {'class': 'focus:ring-indigo-500 focus:border-indigo-500 block w-full pl-7 pr-12 sm:text-sm border-gray-300 rounded-md'}}) }}
        </div>
    </div>

    <div class="px-4 py-3 text-right sm:px-6">
        {{ form_widget(Form.add, {'attr': {'class': 'inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500'}}) }}
    </div>

    {{ form_end(Form) }}

{% endblock %}
